interface:
  display_name: "CodeBuddy GPT & Gemini Bridge"
  short_description: "Securely connect subscription GPT and Gemini models while protecting built-in models"
  default_prompt: "Use $codebuddy-gpt-gemini-bridge to securely connect my GPT and Gemini subscription models to CodeBuddy, and verify that built-in models are unaffected."
